# bundle

> **This branch is generated output. Do not hand-edit.**
>
> Contents are assembled automatically by `scripts/build-bundle.mjs`.
> Re-run the script (or let CI run it) to regenerate.
>
> Assembled from: `backend` + `frontend` + `cli` branches of this repository.